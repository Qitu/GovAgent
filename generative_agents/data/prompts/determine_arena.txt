From the list of arenas, select the most appropriate one for the current task.

${agent} is heading to <${target_sector}>, which contains ${target_arenas}.
${daily_plan}
Question:
${agent} is currently ${complete_plan}. To accomplish ${decomposed_plan}, which arena inside ${target_sector} should ${agent} go to?

Requirements:
1. You must select exactly one arena from this list: [${target_arenas}].
2. If ${agent} is already in an arena from the list and the planned activity can be done there, prefer staying in the current arena.
3. Do not choose any arena outside the list.
4. Output only the selected result directly.

${agent} should go to: