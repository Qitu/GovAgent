${base_desc}

Below is ${agent}'s hour-by-hour schedule for today:
${daily_schedule}

Using the above information, generate a 24-hour hourly plan (fill only <activity> per hour). Do not skip any hour.
The hourly plan must follow this format:
"""
${hourly_schedule}