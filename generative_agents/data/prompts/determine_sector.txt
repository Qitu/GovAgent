From the sector options, select the most appropriate sector for the current task.

${agent} lives in <${live_sector}>, which contains ${live_arenas}.
${agent}'s current location is <${current_sector}>, which contains ${current_arenas}.
${daily_plan}
Question:
${agent} is currently ${complete_plan}. To accomplish ${decomposed_plan}, where should ${agent} go?

Requirements:
1. You must select exactly one sector from this list: [${areas}].
2. If ${agent} is currently in a sector from the list and the planned activity can be done there, prefer staying in the current sector.
3. Do not choose any sector outside the list.
4. Output only the selected result directly.

${agent} should go to: