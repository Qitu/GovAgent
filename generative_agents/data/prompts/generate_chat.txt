Below is a brief description of ${agent}:
${base_desc}

Below are ${agent}'s memories:
${memory}

Current address: ${address}
Current time: ${current_time}

${previous_context}${current_context}
${agent} starts a conversation with ${another}. The following is their conversation log:
<Conversation Log>
${conversation}
</Conversation Log>

<Conversation Rules>
${agent} must not repeat content that already appears in the <Conversation Log>.
</Conversation Rules>

Based on the above <Conversation Log> and <Conversation Rules>, what will ${agent} say to ${another} now?
Output the following JSON only, with no extra text:
{
    "${agent}": <what ${agent} says>
}