name: Prompt Tests - decide_chat

on:
  workflow_dispatch:
  push:
    paths:
      - "generative_agents/data/prompts/**"
      - "promptfoo.decide_chat.yaml"
      - "scripts/run_prompt_tests.sh"
      - ".github/workflows/prompt-tests.decide_chat.yml"
  pull_request:
    paths:
      - "generative_agents/data/prompts/**"
      - "promptfoo.decide_chat.yaml"
      - "scripts/run_prompt_tests.sh"

jobs:
  eval-decide-chat:
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY != '' && secrets.OPENAI_API_KEY || secrets.OPENAI_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install promptfoo
        run: |
          npm i -g promptfoo
          promptfoo --version

      - name: Check OPENAI_API_KEY presence
        run: |
          if [ -z "${OPENAI_API_KEY:-}" ]; then
            echo "::error::OPENAI_API_KEY is empty. Configure repository secret OPENAI_API_KEY (or OPENAI_KEY as fallback).";
            exit 1;
          fi
          echo "OPENAI_API_KEY length: ${#OPENAI_API_KEY} (masked)"

      - name: Run decide_chat prompt tests
        run: |
          bash scripts/run_prompt_tests.sh promptfoo.decide_chat.yaml decide_chat.json

      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: promptfoo-decide_chat
          path: results/promptfoo/decide_chat.json
